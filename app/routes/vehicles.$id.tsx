import type { Route } from "./+types/vehicles.$id";

export default function VehicleDetail(_props: Route.ComponentProps) {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold">Vehicle Details</h1>
      <p className="text-muted-foreground">This page is empty. You can implement the detail view later.</p>
    </main>
  );
}
